-RAM-
STACK - 변수(int a. 이름으로 접근) , 메소드 호출시 > 지역변수
HEAP - 변수(new.  !주소값으로 접근!), 배열, 
             > 인스턴스
DATA - 값이 변하면 안됨(읽기만 가능). 상수
CODE - 명령어 

(데이터 타입 - 구조, 길이) + 주소 = 변수선언

모든 객체는 참조변수로만 접근할 수 있다.
생성자는 메소드
